using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using RentItServer.ITU;
using RentItServer;

namespace RentItServer_UnitTests.ItuTests
{
    [TestClass]
    public class StreamHandlerTest
    {
        [TestMethod]
        public void TestStartStream()
        {/*
            User u = Controller.GetInstance().SignUp("Coolguy", "hattrick@hotmail.com", "12345");
            Controller.GetInstance().CreateChannel("Pop Songs", u.Id, "Latest hits", 
            StreamHandler.GetInstance().StartStream
        */
        }
    }
}
